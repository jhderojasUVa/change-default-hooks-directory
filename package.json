{
  "name": "change-default-hooks-directory",
  "version": "1.0.0",
  "description": "A repository with hooks for conventional commits and branch enforced",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepare": "husky"
  },
  "author": "N/A",
  "license": "ISC",
  "devDependencies": {
    "@commitlint/cli": "^18.6.0",
    "@commitlint/config-conventional": "^18.6.0",
    "husky": "^9.0.10"
  }
}
